service : {
  getTranslations: () -> (vec record {
                                text;
                                text;
                                text;
                              }) query;
  storeTranslation: (text, text, text) -> ();
}
